import { Box, VStack } from "@chakra-ui/react";

export const LargeHamburgerIcon = (props) => (
  <VStack className={"menu-icon"}>
    <Box className="bar1"></Box>
    <Box className="bar2"></Box>
    <Box className="bar3"></Box>
  </VStack>
);
